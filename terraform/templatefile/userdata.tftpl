# common to all instance roles
%{ for enabled in is_k8s ~}
# this is only for k8s
%{ endfor ~}
${ instance_role != null ? "special" : "not special" }
%{ if instance_role == "special" ~}
# this is only for other special roles
%{ endif ~}
